group 'tkom'
version '1.0-SNAPSHOT'

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    compile project(":interfaces")
    compile project(":implementations")
    compile project(":tests")
}
